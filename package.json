{
  "name": "electron-auto-update-example",
  "version": "1.0.0",
  "description": "Electron app with electron-builder and electron-updater",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder build --mac --win --publish never",
    "deploy": "electron-builder build --mac --win --publish always"
  },
  "keywords": [
    "electron"
  ],
  "author": "Michael Evans",
  "license": "MIT",
  "repository": {
    "type" : "git",
    "url" : "https://github.com/mdevans/electron-auto-update-example.git"
  },
  "devDependencies": {
    "electron": "^24.3.0",
    "electron-builder": "^24.4.0"
  },
  "dependencies": {
    "electron-updater": "^5.3.0"
  }
}
